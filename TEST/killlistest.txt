X-Render system for galaxy view
X-Render system for star system view
 -Bodies
   X-Planet
    -Asteroids
    -Starbase/ports
    -Other space stations
X-Crude ship movement
   X-Move to pin
    -Dashed line for movement path
    -System jump
    -Fleet
      -Fleet follows point ship
      -Maintain offset
 -Action menu
     -Move here
     -View
     -Grey out invalid options
 -Resource Nodes
 -Improved System generation
   -Resource nodes generate by region
   -NPC ships generate  
-System view neighbor system arrows
      -Flying to arrow warps to system
      -NPC ships flying through approach arrows
-System tick function
    -2fps tick for animation
    -1 tick per 30 sec for chance of event
      -NPC event type / frequency dependent on system type
      -NPC ships spawn, fly through
      -NPC ships spawn, fly to random node / station
      -Existing NPC fleets move / leave
    -1 tick per 15 sec for body updates
-System types:
  -PvP
    -T1-T10 nodes
  -GG
      -No nodes
  -PvE
      -T1-T6 nodes
-System type render tints
X-Update drawTriangle() to be more flexible
-Galaxy view ticks
     -Once 30 sec updates
     -Once per 10 sec events
         -NPC ships rendered smaller, fly between systems
-Update planet rotation by 24hr cycle (new Date().getHours())